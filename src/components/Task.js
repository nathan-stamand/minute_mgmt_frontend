import React, { useState } from "react";

function Task(props) {
  const [completed, setCompleted] = useState(false);
  const [timerOn, setTimerOn] = useState(false);

  return(
    <div className="compressed task">
      
    </div>
  )
};

export default Task;